function updateBooks() {
    // let usersJSON = JQuery.getJSON(URL_LOCALHOST + "/user/users");
    // $.each(usersJSON.items, function (i, item) {
    //     console.log(item);
    // });
    // //console.log(parsedJSON);
}